package com.health.data;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.health.data.Moon;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table tblmoon.
*/
public class MoonDao extends AbstractDao<Moon, Long> {

    public static final String TABLENAME = "tblmoon";

    /**
     * Properties of entity Moon.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "id");
        public final static Property ServerId = new Property(1, Long.class, "ServerId", false, "serverid");
        public final static Property RowId = new Property(2, String.class, "RowId", false, "rowid");
        public final static Property InfoDate = new Property(3, java.util.Date.class, "InfoDate", false, "dt");
        public final static Property MoonVisibility = new Property(4, Double.class, "MoonVisibility", false, "MOON_VISIBILITY");
        public final static Property MoonOld = new Property(5, java.util.Date.class, "MoonOld", false, "MOON_OLD");
        public final static Property MoonRise = new Property(6, java.util.Date.class, "MoonRise", false, "MOON_RISE");
        public final static Property MoonSet = new Property(7, java.util.Date.class, "MoonSet", false, "MOON_SET");
        public final static Property MoonPhaseId = new Property(8, Integer.class, "MoonPhaseId", false, "MOON_PHASE_ID");
    };


    public MoonDao(DaoConfig config) {
        super(config);
    }
    
    public MoonDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'tblmoon' (" + //
                "'id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'serverid' INTEGER UNIQUE ," + // 1: ServerId
                "'rowid' TEXT UNIQUE ," + // 2: RowId
                "'dt' INTEGER," + // 3: InfoDate
                "'MOON_VISIBILITY' REAL," + // 4: MoonVisibility
                "'MOON_OLD' INTEGER," + // 5: MoonOld
                "'MOON_RISE' INTEGER," + // 6: MoonRise
                "'MOON_SET' INTEGER," + // 7: MoonSet
                "'MOON_PHASE_ID' INTEGER);"); // 8: MoonPhaseId
        // Add Indexes
        db.execSQL("CREATE INDEX " + constraint + "IDX_tblmoon_serverid ON tblmoon" +
                " (serverid);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'tblmoon'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Moon entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long ServerId = entity.getServerId();
        if (ServerId != null) {
            stmt.bindLong(2, ServerId);
        }
 
        String RowId = entity.getRowId();
        if (RowId != null) {
            stmt.bindString(3, RowId);
        }
 
        java.util.Date InfoDate = entity.getInfoDate();
        if (InfoDate != null) {
            stmt.bindLong(4, InfoDate.getTime());
        }
 
        Double MoonVisibility = entity.getMoonVisibility();
        if (MoonVisibility != null) {
            stmt.bindDouble(5, MoonVisibility);
        }
 
        java.util.Date MoonOld = entity.getMoonOld();
        if (MoonOld != null) {
            stmt.bindLong(6, MoonOld.getTime());
        }
 
        java.util.Date MoonRise = entity.getMoonRise();
        if (MoonRise != null) {
            stmt.bindLong(7, MoonRise.getTime());
        }
 
        java.util.Date MoonSet = entity.getMoonSet();
        if (MoonSet != null) {
            stmt.bindLong(8, MoonSet.getTime());
        }
 
        Integer MoonPhaseId = entity.getMoonPhaseId();
        if (MoonPhaseId != null) {
            stmt.bindLong(9, MoonPhaseId);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Moon readEntity(Cursor cursor, int offset) {
        Moon entity = new Moon( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // ServerId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // RowId
            cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)), // InfoDate
            cursor.isNull(offset + 4) ? null : cursor.getDouble(offset + 4), // MoonVisibility
            cursor.isNull(offset + 5) ? null : new java.util.Date(cursor.getLong(offset + 5)), // MoonOld
            cursor.isNull(offset + 6) ? null : new java.util.Date(cursor.getLong(offset + 6)), // MoonRise
            cursor.isNull(offset + 7) ? null : new java.util.Date(cursor.getLong(offset + 7)), // MoonSet
            cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8) // MoonPhaseId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Moon entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setServerId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setRowId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setInfoDate(cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)));
        entity.setMoonVisibility(cursor.isNull(offset + 4) ? null : cursor.getDouble(offset + 4));
        entity.setMoonOld(cursor.isNull(offset + 5) ? null : new java.util.Date(cursor.getLong(offset + 5)));
        entity.setMoonRise(cursor.isNull(offset + 6) ? null : new java.util.Date(cursor.getLong(offset + 6)));
        entity.setMoonSet(cursor.isNull(offset + 7) ? null : new java.util.Date(cursor.getLong(offset + 7)));
        entity.setMoonPhaseId(cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Moon entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Moon entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
