package com.health.data;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.health.data.City;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table tblcity.
*/
public class CityDao extends AbstractDao<City, Long> {

    public static final String TABLENAME = "tblcity";

    /**
     * Properties of entity City.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "id");
        public final static Property Name = new Property(1, String.class, "name", false, "name");
        public final static Property CountryCode = new Property(2, String.class, "CountryCode", false, "country_code");
        public final static Property Name_ru = new Property(3, String.class, "Name_ru", false, "name_ru");
        public final static Property Lat = new Property(4, Double.class, "Lat", false, "lat");
        public final static Property Lng = new Property(5, Double.class, "Lng", false, "lng");
        public final static Property CountryId = new Property(6, Long.class, "CountryId", false, "country_id");
    };

    private DaoSession daoSession;


    public CityDao(DaoConfig config) {
        super(config);
    }
    
    public CityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'tblcity' (" + //
                "'id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'name' TEXT," + // 1: name
                "'country_code' TEXT," + // 2: CountryCode
                "'name_ru' TEXT," + // 3: Name_ru
                "'lat' REAL," + // 4: Lat
                "'lng' REAL," + // 5: Lng
                "'country_id' INTEGER);"); // 6: CountryId
        // Add Indexes
        db.execSQL("CREATE INDEX " + constraint + "IDX_tblcity_country_id ON tblcity" +
                " (country_id);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'tblcity'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, City entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String CountryCode = entity.getCountryCode();
        if (CountryCode != null) {
            stmt.bindString(3, CountryCode);
        }
 
        String Name_ru = entity.getName_ru();
        if (Name_ru != null) {
            stmt.bindString(4, Name_ru);
        }
 
        Double Lat = entity.getLat();
        if (Lat != null) {
            stmt.bindDouble(5, Lat);
        }
 
        Double Lng = entity.getLng();
        if (Lng != null) {
            stmt.bindDouble(6, Lng);
        }
 
        Long CountryId = entity.getCountryId();
        if (CountryId != null) {
            stmt.bindLong(7, CountryId);
        }
    }

    @Override
    protected void attachEntity(City entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public City readEntity(Cursor cursor, int offset) {
        City entity = new City( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // CountryCode
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // Name_ru
            cursor.isNull(offset + 4) ? null : cursor.getDouble(offset + 4), // Lat
            cursor.isNull(offset + 5) ? null : cursor.getDouble(offset + 5), // Lng
            cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6) // CountryId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, City entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setCountryCode(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setName_ru(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setLat(cursor.isNull(offset + 4) ? null : cursor.getDouble(offset + 4));
        entity.setLng(cursor.isNull(offset + 5) ? null : cursor.getDouble(offset + 5));
        entity.setCountryId(cursor.isNull(offset + 6) ? null : cursor.getLong(offset + 6));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(City entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(City entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getCountryDao().getAllColumns());
            builder.append(" FROM tblcity T");
            builder.append(" LEFT JOIN tblcountry T0 ON T.'country_id'=T0.'id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected City loadCurrentDeep(Cursor cursor, boolean lock) {
        City entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Country country = loadCurrentOther(daoSession.getCountryDao(), cursor, offset);
        entity.setCountry(country);

        return entity;    
    }

    public City loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<City> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<City> list = new ArrayList<City>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<City> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<City> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
