package com.health.data;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.health.data.WeatherDaily;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table tblweatherdaily.
*/
public class WeatherDailyDao extends AbstractDao<WeatherDaily, Long> {

    public static final String TABLENAME = "tblweatherdaily";

    /**
     * Properties of entity Weather.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "id");
        public final static Property ServerId = new Property(1, Long.class, "ServerId", false, "serverid");
        public final static Property RowId = new Property(2, String.class, "RowId", false, "rowid");
        public final static Property InfoDate = new Property(3, java.util.Date.class, "InfoDate", false, "dt");
        public final static Property MinTemperature = new Property(4, Double.class, "MinTemperature", false, "MIN_TEMPERATURE");
        public final static Property MaxTemperature = new Property(5, Double.class, "MaxTemperature", false, "MAX_TEMPERATURE");
        public final static Property Pressure = new Property(6, Double.class, "Pressure", false, "PRESSURE");
        public final static Property Humidity = new Property(7, Double.class, "Humidity", false, "HUMIDITY");
        public final static Property WindSpeed = new Property(8, Double.class, "WindSpeed", false, "WIND_SPEED");
        public final static Property WindDeg = new Property(9, Double.class, "WindDeg", false, "WIND_DEG");
        public final static Property Clouds = new Property(10, Double.class, "Clouds", false, "CLOUDS");
        public final static Property Rain = new Property(11, Double.class, "Rain", false, "RAIN");
        public final static Property Snow = new Property(12, Double.class, "Snow", false, "SNOW");
    };


    public WeatherDailyDao(DaoConfig config) {
        super(config);
    }
    
    public WeatherDailyDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'tblweatherdaily' (" + //
                "'id' INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "'serverid' INTEGER UNIQUE ," + // 1: ServerId
                "'rowid' TEXT UNIQUE ," + // 2: RowId
                "'dt' INTEGER," + // 3: InfoDate
                "'MIN_TEMPERATURE' REAL," + // 4: MinTemperature
                "'MAX_TEMPERATURE' REAL," + // 5: MaxTemperature
                "'PRESSURE' REAL," + // 6: Pressure
                "'HUMIDITY' REAL," + // 7: Humidity
                "'WIND_SPEED' REAL," + // 8: WindSpeed
                "'WIND_DEG' REAL," + // 9: WindDeg
                "'CLOUDS' REAL," + // 10: Clouds
                "'RAIN' REAL," + // 11: Rain
                "'SNOW' REAL);"); // 12: Snow
        // Add Indexes
        db.execSQL("CREATE INDEX " + constraint + "IDX_tblweatherdaily_serverid ON tblweatherdaily" +
                " (serverid);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'tblweatherdaily'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, WeatherDaily entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long ServerId = entity.getServerId();
        if (ServerId != null) {
            stmt.bindLong(2, ServerId);
        }
 
        String RowId = entity.getRowId();
        if (RowId != null) {
            stmt.bindString(3, RowId);
        }
 
        java.util.Date InfoDate = entity.getInfoDate();
        if (InfoDate != null) {
            stmt.bindLong(4, InfoDate.getTime());
        }
 
        Double MinTemperature = entity.getMinTemperature();
        if (MinTemperature != null) {
            stmt.bindDouble(5, MinTemperature);
        }
 
        Double MaxTemperature = entity.getMaxTemperature();
        if (MaxTemperature != null) {
            stmt.bindDouble(6, MaxTemperature);
        }
 
        Double Pressure = entity.getPressure();
        if (Pressure != null) {
            stmt.bindDouble(7, Pressure);
        }
 
        Double Humidity = entity.getHumidity();
        if (Humidity != null) {
            stmt.bindDouble(8, Humidity);
        }
 
        Double WindSpeed = entity.getWindSpeed();
        if (WindSpeed != null) {
            stmt.bindDouble(9, WindSpeed);
        }
 
        Double WindDeg = entity.getWindDeg();
        if (WindDeg != null) {
            stmt.bindDouble(10, WindDeg);
        }
 
        Double Clouds = entity.getClouds();
        if (Clouds != null) {
            stmt.bindDouble(11, Clouds);
        }
 
        Double Rain = entity.getRain();
        if (Rain != null) {
            stmt.bindDouble(12, Rain);
        }
 
        Double Snow = entity.getSnow();
        if (Snow != null) {
            stmt.bindDouble(13, Snow);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public WeatherDaily readEntity(Cursor cursor, int offset) {
        WeatherDaily entity = new WeatherDaily( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1), // ServerId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // RowId
            cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)), // InfoDate
            cursor.isNull(offset + 4) ? null : cursor.getDouble(offset + 4), // MinTemperature
            cursor.isNull(offset + 5) ? null : cursor.getDouble(offset + 5), // MaxTemperature
            cursor.isNull(offset + 6) ? null : cursor.getDouble(offset + 6), // Pressure
            cursor.isNull(offset + 7) ? null : cursor.getDouble(offset + 7), // Humidity
            cursor.isNull(offset + 8) ? null : cursor.getDouble(offset + 8), // WindSpeed
            cursor.isNull(offset + 9) ? null : cursor.getDouble(offset + 9), // WindDeg
            cursor.isNull(offset + 10) ? null : cursor.getDouble(offset + 10), // Clouds
            cursor.isNull(offset + 11) ? null : cursor.getDouble(offset + 11), // Rain
            cursor.isNull(offset + 12) ? null : cursor.getDouble(offset + 12) // Snow
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, WeatherDaily entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setServerId(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setRowId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setInfoDate(cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)));
        entity.setMinTemperature(cursor.isNull(offset + 4) ? null : cursor.getDouble(offset + 4));
        entity.setMaxTemperature(cursor.isNull(offset + 5) ? null : cursor.getDouble(offset + 5));
        entity.setPressure(cursor.isNull(offset + 6) ? null : cursor.getDouble(offset + 6));
        entity.setHumidity(cursor.isNull(offset + 7) ? null : cursor.getDouble(offset + 7));
        entity.setWindSpeed(cursor.isNull(offset + 8) ? null : cursor.getDouble(offset + 8));
        entity.setWindDeg(cursor.isNull(offset + 9) ? null : cursor.getDouble(offset + 9));
        entity.setClouds(cursor.isNull(offset + 10) ? null : cursor.getDouble(offset + 10));
        entity.setRain(cursor.isNull(offset + 11) ? null : cursor.getDouble(offset + 11));
        entity.setSnow(cursor.isNull(offset + 12) ? null : cursor.getDouble(offset + 12));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(WeatherDaily entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(WeatherDaily entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
